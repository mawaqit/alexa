# Used by `vite build --mode dev` (see scripts/deploy-web.js). Not secret —
# just a public API endpoint — safe to commit. Printed under "endpoints:" in
# `lambda/`'s deploy output (or `pnpm --filter ./lambda exec serverless info
# --stage dev`) — won't change between deploys unless the function is torn
# down and recreated.
VITE_API_BASE_URL=https://6vxlp3jpkienv4nfg7sh4g7a4e0wodxt.lambda-url.eu-west-3.on.aws
