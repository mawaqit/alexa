**/node_modules

# Serverless Framework build/deploy artifacts (esbuild bundles, zips, state)
**/.serverless

# Jest coverage output (`jest --coverage`)
coverage

# Vite build output (web/)
web/dist
web/.env.local
