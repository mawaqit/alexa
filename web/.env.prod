# Used by `vite build --mode prod` (see scripts/deploy-web.js). Not secret —
# just a public API endpoint — safe to commit.
#
# TODO: fill in after lambda/ has been deployed to the prod stage at least
# once. The URL is printed under "endpoints:" in that deploy's output
# (or `pnpm --filter ./lambda exec serverless info --stage prod`). It won't
# change between deploys once set, so this only needs doing once.
VITE_API_BASE_URL=
